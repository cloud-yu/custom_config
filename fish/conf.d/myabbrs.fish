# git abbr
abbr -a --position command -- gst git status

# grep abbr to enable color
#abbr -a --position command -- grep grep --color=auto

# ip route alias for ipv6
abbr -a --position command -- ip6 ip -6
